import type { IAddressSpace } from "node-opcua-address-space-base";
import { Variant } from "node-opcua-variant";
export declare function resolveOpaqueOnAddressSpace(addressSpace: IAddressSpace, variants: (Variant | null) | (Variant | null)[]): Promise<void>;
