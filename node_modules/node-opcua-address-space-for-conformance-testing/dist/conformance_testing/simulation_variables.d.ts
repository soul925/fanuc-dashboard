/**
 * Simulation (dynamic) variables that change randomly on a timer.
 */
import type { Namespace, UAObject } from "node-opcua-address-space";
export declare function addSimulationVariables(namespace: Namespace, scalarFolder: UAObject): void;
